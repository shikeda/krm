## データの基本ルール

- `krm.db` は読み取り専用。直接編集・変更禁止
- TSVファイルの先頭行（`#`で始まる行）はコメント行
- `entry_id`（例: `F00001`）が全テーブルの起点
- 見出し字は康熙字典体が原則。未符号化文字はIDS（例: `⿰氵人`）または `■` で表記

## 外部データ（要別途入手）

以下のファイルはライセンス上リポジトリに含めず、別途入手が必要：

| ファイル | 入手先 | 用途 |
|---|---|---|
| `異体漢字対応テーブル111220版_TSV221111.txt` | https://www.bridge.nihu.jp/researchdata/file/20221125_ITOBYb | 異体字曖昧検索（CC-BY 4.0） |

## webappの詳細

`webapp/` ディレクトリを参照。
エージェント向け追加ルールは `webapp/AGENTS.md` に記載。
